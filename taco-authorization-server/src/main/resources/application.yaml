server:
  port: 9000
  ssl:
    enabled: false

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/oauth
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update